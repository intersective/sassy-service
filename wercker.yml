box: node

build:
    steps:
        - npm-install

init:
    steps:
        - claudia generate-serverless-express-proxy --express-module index
        - claudia create --handler lambda.handler --deploy-proxy-api --region $AWS_REGION

deploy:
    steps:
        - npm run update
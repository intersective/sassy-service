box: node

build:
    steps:
        - npm-install

init:
    steps:
        - script:
            name: initialisation
            code: |
                npm run generate-proxy
                npm run deploy

deploy:
    steps:
        - script:
            name: update 
            code: |
                npm run update